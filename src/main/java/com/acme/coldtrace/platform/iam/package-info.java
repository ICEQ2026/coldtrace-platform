/**
 * IAM context.
 *
 * <p>The current implementation covers organizations, users, roles, and
 * permissions as data APIs. Authentication, JWT, protected routes, password
 * reset requests, and external identity providers are owned by explicit
 * IAM/security stories.</p>
 */
package com.acme.coldtrace.platform.iam;
